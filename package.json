{
  "name": "glitchinarium",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun build ./src/render/worker.js --outfile=./worker.js --target=browser && bun --hot index.html",
    "dev:watch-worker": "bun build ./src/render/worker.js --outfile=./worker.js --target=browser --watch",
    "build": "bun scripts/build.js",
    "verify": "bun verify.js"
  },
  "dependencies": {
    "p5": "^2.0.5"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.3"
  }
}
